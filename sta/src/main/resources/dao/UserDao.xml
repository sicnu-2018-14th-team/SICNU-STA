<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sicnu.sta.dao.UserDao">
    <select id="judgeUserLogin" resultType="User">
        SELECT * FROM user WHERE email = #{email} AND password = #{password}
    </select>

    <insert id="insertNewUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user(email, password, userName) VALUES (#{user.email}, #{user.password}, #{user.userName})
    </insert>

    <select id="findUserByEmail" resultType="User">
        SELECT userId, email, password FROM user WHERE email = #{email} limit 1
    </select>

    <update id="updateUserPassword">
        UPDATE user SET password = #{password} WHERE email = #{email}
    </update>

</mapper>