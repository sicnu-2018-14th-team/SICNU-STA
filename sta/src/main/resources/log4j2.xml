<?xml version="1.0" encoding="UTF-8"?>
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<configuration monitorInterval="5">
    <!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->

    <!--变量配置-->
    <Properties>
        <!-- 格式化输出：%date表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度 %msg：日志消息，%n是换行符-->
        <!-- %logger{36} 表示 Logger 名字最长36个字符 -->
        <property name="LOG_PATTERN" value="%date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />
        <!-- 定义日志存储的路径 -->
        <property name="FILE_PATH" value="C://log4j//logs//log//dev" />
        <property name="FILE_NAME" value="log4j2Demo" />
    </Properties>

    <appenders>

        <console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <!--控制台只输出level及其以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
        </console>

        <!--保存到数据库配置文件-->
        <JDBC name="databaseAppenderSysLog" tableName="sys_log">
            <ConnectionFactory class="com.sicnu.sta.config.ConnectionFactoryConfig" method="getDatabaseConnection"/>
            <Column name="date" pattern="%date{YYYY-MM-dd HH:mm:ss}"/>'
            <Column name="class_name" pattern="%c"/>
            <Column name="thread_name" pattern="%thread"/>
            <Column name="level" pattern="%level"/>
            <Column name="logger_name" pattern="%logger"/>
            <Column name="message" pattern="%m"/>
        </JDBC>

    </appenders>

    <!--Logger节点用来单独指定日志的形式，比如要为指定包下的class指定不同的日志级别等。-->
    <!--然后定义loggers，只有定义了logger并引入的appender，appender才会生效-->
    <loggers>

        <!--过滤掉spring和mybatis的一些无用的DEBUG信息-->
        <logger name="org.mybatis" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </logger>

        <logger name="com.alibaba.druid.pool.DruidDataSource" level="info" additivity="false">
        </logger>
        <!--监控系统信息-->
        <!--若是additivity设为false，则 子Logger 只会在自己的appender里输出，而不会在 父Logger 的appender里输出。-->
        <Logger name="org.springframework" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <logger name="com.sicnu.sta.service" level="info">
            <AppenderRef ref="databaseAppenderSysLog"/>
        </logger>

        <root level="info">
            <appender-ref ref="Console"/>
            <AppenderRef ref="databaseAppenderSysLog"/>
        </root>
    </loggers>

</configuration>